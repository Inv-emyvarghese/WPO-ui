import { ExecutiveRecommendationsSection } from "@/components/dashboard/tab/ExecutiveRecommendationsSection";

/** Narrative / guidance accordion: same content as the recommendations block used on the executive overview. */
export function NarrativeRecommendations() {
  return <ExecutiveRecommendationsSection />;
}
